#SConscript('SConscript',
#    build_dir='.build/release',
#    duplicate=0,
#    exports={'MODE':'release'})


SConscript(
    'src/calibration/SConscript',
    variant_dir='build/debug',
    src_dir='src',
    duplicate=0,
    exports={'MODE':'debug'})

SConscript(
    'src/grab/SConscript',
    variant_dir='build/debug',
    src_dir='src',
    duplicate=0,
    exports={'MODE':'debug'})

SConscript(
    'src/matching/SConscript',
    variant_dir='build/debug',
    src_dir='src',
    duplicate=0,
    exports={'MODE':'debug'})

#SConscript(
#    'src/pointcloud/SConscript',
#    variant_dir='build/debug',
#    src_dir='src',
#    duplicate=0,
#    exports={'MODE':'debug'})

SConscript(
    'src/tuner/SConscript',
    variant_dir='build/debug',
    src_dir='src',
    duplicate=0,
    exports={'MODE':'debug'})
